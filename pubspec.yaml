name: glowguard_backend
description: GlowGuard Chatbot backend proxy (Dart + Shelf)
version: 1.0.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5
  http: ^1.2.2